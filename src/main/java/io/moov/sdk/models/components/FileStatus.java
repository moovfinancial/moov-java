/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package io.moov.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * FileStatus
 * 
 * <p>The file's status.
 */
public class FileStatus {

    public static final FileStatus PENDING = new FileStatus("pending");
    public static final FileStatus APPROVED = new FileStatus("approved");
    public static final FileStatus REJECTED = new FileStatus("rejected");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, FileStatus> values = createValuesMap();
    private static final Map<String, FileStatusEnum> enums = createEnumsMap();

    private final String value;

    private FileStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a FileStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as FileStatus
     */ 
    @JsonCreator
    public static FileStatus of(String value) {
        synchronized (FileStatus.class) {
            return values.computeIfAbsent(value, v -> new FileStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<FileStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        FileStatus other = (FileStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "FileStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static FileStatus[] values() {
        synchronized (FileStatus.class) {
            return values.values().toArray(new FileStatus[] {});
        }
    }

    private static final Map<String, FileStatus> createValuesMap() {
        Map<String, FileStatus> map = new LinkedHashMap<>();
        map.put("pending", PENDING);
        map.put("approved", APPROVED);
        map.put("rejected", REJECTED);
        return map;
    }

    private static final Map<String, FileStatusEnum> createEnumsMap() {
        Map<String, FileStatusEnum> map = new HashMap<>();
        map.put("pending", FileStatusEnum.PENDING);
        map.put("approved", FileStatusEnum.APPROVED);
        map.put("rejected", FileStatusEnum.REJECTED);
        return map;
    }
    
    
    public enum FileStatusEnum {

        PENDING("pending"),
        APPROVED("approved"),
        REJECTED("rejected"),;

        private final String value;

        private FileStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

