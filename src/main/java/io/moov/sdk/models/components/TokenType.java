/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package io.moov.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * TokenType
 * 
 * <p>An [RFC 6750](https://www.rfc-editor.org/rfc/rfc6750#section-6.1) token type.
 */
public class TokenType {

    public static final TokenType BEARER = new TokenType("Bearer");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, TokenType> values = createValuesMap();
    private static final Map<String, TokenTypeEnum> enums = createEnumsMap();

    private final String value;

    private TokenType(String value) {
        this.value = value;
    }

    /**
     * Returns a TokenType with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as TokenType
     */ 
    @JsonCreator
    public static TokenType of(String value) {
        synchronized (TokenType.class) {
            return values.computeIfAbsent(value, v -> new TokenType(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<TokenTypeEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        TokenType other = (TokenType) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "TokenType [value=" + value + "]";
    }

    // return an array just like an enum
    public static TokenType[] values() {
        synchronized (TokenType.class) {
            return values.values().toArray(new TokenType[] {});
        }
    }

    private static final Map<String, TokenType> createValuesMap() {
        Map<String, TokenType> map = new LinkedHashMap<>();
        map.put("Bearer", BEARER);
        return map;
    }

    private static final Map<String, TokenTypeEnum> createEnumsMap() {
        Map<String, TokenTypeEnum> map = new HashMap<>();
        map.put("Bearer", TokenTypeEnum.BEARER);
        return map;
    }
    
    
    public enum TokenTypeEnum {

        BEARER("Bearer"),;

        private final String value;

        private TokenTypeEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

