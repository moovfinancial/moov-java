/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package io.moov.sdk.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
/**
 * WalletStatus
 * 
 * <p>Status of a wallet.
 * - `active`: The wallet is available for use and has an enabled payment method.
 * - `closed`: The wallet is no longer active and the corresponding payment method has been disabled.
 */
public class WalletStatus {

    public static final WalletStatus ACTIVE = new WalletStatus("active");
    public static final WalletStatus CLOSED = new WalletStatus("closed");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, WalletStatus> values = createValuesMap();
    private static final Map<String, WalletStatusEnum> enums = createEnumsMap();

    private final String value;

    private WalletStatus(String value) {
        this.value = value;
    }

    /**
     * Returns a WalletStatus with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as WalletStatus
     */ 
    @JsonCreator
    public static WalletStatus of(String value) {
        synchronized (WalletStatus.class) {
            return values.computeIfAbsent(value, v -> new WalletStatus(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<WalletStatusEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        WalletStatus other = (WalletStatus) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "WalletStatus [value=" + value + "]";
    }

    // return an array just like an enum
    public static WalletStatus[] values() {
        synchronized (WalletStatus.class) {
            return values.values().toArray(new WalletStatus[] {});
        }
    }

    private static final Map<String, WalletStatus> createValuesMap() {
        Map<String, WalletStatus> map = new LinkedHashMap<>();
        map.put("active", ACTIVE);
        map.put("closed", CLOSED);
        return map;
    }

    private static final Map<String, WalletStatusEnum> createEnumsMap() {
        Map<String, WalletStatusEnum> map = new HashMap<>();
        map.put("active", WalletStatusEnum.ACTIVE);
        map.put("closed", WalletStatusEnum.CLOSED);
        return map;
    }
    
    
    public enum WalletStatusEnum {

        ACTIVE("active"),
        CLOSED("closed"),;

        private final String value;

        private WalletStatusEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

